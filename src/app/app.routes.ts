import { Routes } from '@angular/router';

// Import of pages
import { Inicio } from './pages/inicio/inicio';

export const routes: Routes = [
    {
        path: '',
        component: Inicio
    }
];
